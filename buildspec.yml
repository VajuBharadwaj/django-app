version: 0.2

phases:
  install:
    commands:
      - pip install -r requirements.txt
  pre_build:
    commands:
      - python manage.py test
  build:
    commands:
      - echo "Build and test completed"





